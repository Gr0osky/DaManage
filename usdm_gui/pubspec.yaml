name: usdm_gui
description: "DaManage secure password manager UI"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  flutter_secure_storage: ^9.2.2
  window_manager: ^0.5.1
  provider: ^6.1.1
  shared_preferences: ^2.3.4
  local_auth: ^2.2.0
  crypto: ^3.0.3
  pointycastle: ^3.9.1
  convert: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/images/
